name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  id-token: write # required to request the GitHub OIDC token for NuGet Trusted Publishing
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Derive version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal

      # TierGate.Core and TierGate.AspNetCore are versioned together, one number per
      # release — AspNetCore's packed dependency on Core needs to resolve to a version
      # that's actually published, so keeping them in lockstep avoids ordering issues.
      - name: Pack
        run: |
          dotnet pack src/TierGate.Core/TierGate.Core.csproj \
            --configuration Release \
            -p:PackageVersion=${{ steps.version.outputs.version }} \
            -o ./artifacts
          dotnet pack src/TierGate.AspNetCore/TierGate.AspNetCore.csproj \
            --configuration Release \
            -p:PackageVersion=${{ steps.version.outputs.version }} \
            -o ./artifacts

      - name: NuGet login (OIDC -> short-lived API key)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Push to NuGet
        run: |
          dotnet nuget push ./artifacts/*.nupkg \
            --api-key ${{ steps.login.outputs.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
